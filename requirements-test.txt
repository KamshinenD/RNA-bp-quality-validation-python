# Testing dependencies for RNA structure quality scoring

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0

# For mocking in tests
pytest-mock>=3.10.0

# Required dependencies (also needed for main code)
pandas>=1.5.0
requests>=2.28.0
